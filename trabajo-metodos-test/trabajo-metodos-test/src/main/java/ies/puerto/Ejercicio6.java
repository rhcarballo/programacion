package ies.puerto;

public class Ejercicio6 {
    
    /**
     * recorrer un raango de numeros (1..1000)
     * para un numero comprobar si Amstrong
     * Si es imprime en pantalla
     * si no sigue adelante
     */
    
     /**
      * Funcion que verifica si numero es Amstrong
      * @param numero para realizar cerificacion
      * @return
      */
     public boolean verficaAmstrong(int numero) {
       
        int numDigitos = 0;
        int calcularDigitos = numero;
        while (calcularDigitos > 0){
            calcularDigitos = calcularDigitos/10;
        }

        for (int i = 0; i < numDigitos; i++) {
            suma = suma
        }



        return resultado;
     }
     
}